using UnityEngine;
using System.Collections;

public class RayPoint : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //var child = gameObject.transform.FindChild("RayCircularSector");
        //child.localRotation = Quaternion.Euler(
        //    new Vector3(
        //    0.0f,
        //    -transform.parent.rotation.eulerAngles.y + transform.parent.rotation.eulerAngles.y / 2,
        //    0.0f)
        //    );
    }
}
